# LocalTools Suite — Privacy Policy

This public repository hosts the privacy policy for **LocalTools Suite**, a privacy-focused offline Windows desktop utility.

The published policy is intended for the Microsoft Store listing and reflects the application's documented local-data behavior for version 4.0.0.

GitHub Pages should be configured to publish from the `main` branch and repository root (`/`).
